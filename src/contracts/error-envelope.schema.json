{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://password-manager-health-check.local/contracts/error-envelope.schema.json",
  "title": "ErrorEnvelope",
  "description": "Universal non-2xx response shape for both the FastAPI agent and the Next.js BFF (Constitution IV + VII).",
  "type": "object",
  "required": ["code", "message"],
  "additionalProperties": false,
  "properties": {
    "code": {
      "type": "string",
      "enum": [
        "INVALID_INPUT",
        "AGENT_TIMEOUT",
        "AGENT_FORMAT_ERROR",
        "RATE_LIMITED",
        "INTERNAL_ERROR"
      ]
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "maxLength": 280,
      "description": "User-safe text rendered directly to the chat. Never includes stack traces, framework jargon, or internal IDs."
    }
  }
}
